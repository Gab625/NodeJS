<!DOCTYPE html>
<html lang="pt-br">
    <head>
        <meta charset="UTF-8">
        <title>Layout com Sidebar (Flexbox)</title>
        <style>
            * {box-sizing: border-box;}
            body {margin: 0; font-family: Arial, Helvetica, sans-serif;}

            header {
                background: #0b3d91;
                color: #fff;
                padding: 16px 24px;
                font-size: 20px;
            }

            .shell {
                display: flex;
                min-height: calc(100vh - 64px);            
            }

            .sidebar {
                flex: 0 0 260px;
                background: #f4f6f8;
                border-right: 1px solid #e0e0e0;
                padding: 16px;
                display: flex;
                flex-direction: column;
                gap: 8px;
            }

            .sidebar a {
                text-decoration: none;
                color: #0b3d91;
                padding: 10px 12px;
                border-radius: 8px;
                transition: background 0.2s;
            }
            .sidebar a:hover {background: #e8eefc; }

            .content {
                flex: 1 1 auto;
                padding: 24px;
                display: flex;
                flex-direction: column;
                gap: 16px;
                background: #ffffff;
            }

            .card {
                border: 1px solid #e5e7eb;
                border-radius: 10px;
                padding: 16px;
                box-shadow: 0 1px 2px rgba(red, green, blue, alpha);
            }

            footer {
                margin-top: auto;
                color: #555;
                font-size: 14px;
            }

            @media(max-width: 800px) {
                .shell{flex-direction: column;}
                .sidebar {
                    flex: 0 0 auto;
                    border-right: none;
                    border-bottom: 1px solid #e0e0e0;
                }
            }
        </style>
    </head>
    <body>
        <header>Meu Site - Sidebar + Conteúdo (Flexbox)</header>

        <div class="shell">
            <nav class="sidebar">
                <strong>Navegação</strong>
                <a href="#">Início</a>
                <a href="#">Artigos</a>
                <a href="#">Produtos</a>
                <a href="#">Contato</a>
            </nav>

            <main class="content">
                <section class="card">
                    <h2>Título da página</h2>
                    <p>Este é o conteúdo principal. Aqui você coloca textos, imagens, tabelas, formulários etc.<0/p>
                </section>

                <section class="card">
                    <h3>Seção 2</h3>
                    <p>Mais conteúdo, demonstrando como os cartões se comportam dentro do fluxo.</p>
                </section>

                <footer>
                    &copy 2025 - Rodapé da área de conteúdo
                </footer>
            </main>
        </div>
    </body>
</html>